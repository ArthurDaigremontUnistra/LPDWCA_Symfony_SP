{% extends 'base.html.twig' %}

{% block title %}Modifier une transaction{% endblock %}

{% block body %}
    <div class="form">
        <h1>Modifier une transaction</h1>

        {{ form_start(form) }}

            {{ form_row(form.name) }}
            {{ form_row(form.montant) }}
            {{ form_row(form.type) }}
            {{ form_row(form.category) }}
            {{ form_row(form.moyenDePaiement) }}

            <button type="submit">Mettre à jour</button>

        {{ form_end(form) }}

        <div class="btn"><div><a href="{{ path('app_transaction_index') }}">Retour</a></div></div>
    </div>
{% endblock %}
